"""
Адаптеры VPN-панелей.

    base.py             общий контракт: VPNPanelAdapter, NodeSpec, ошибки,
                        детерминированные имена клиентов
    wg_config.py        правка текста конфигурации WireGuard (без сети)
    awg_obfuscation.py  генерация и проверка параметров обфускации AmneziaWG
    amnezia.py          адаптер AlexisHW/amneziawg-web-ui
    factory.py          выбор адаптера по типу панели из строки vpn_nodes

Точка входа для вызывающего кода — `factory.adapter_for(node)`. Имена
классов панелей за её пределами знать не нужно.

Импортов здесь нет намеренно: `amnezia` тянет aiohttp, а миграции, запросы
к базе и разбор конфигураций должны работать без сетевого стека.
"""
